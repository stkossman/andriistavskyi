---
interface Props {
	className?: string
}

const { className = '' } = Astro.props
---

<span class={`inline-flex items-center border border-border px-2.5 py-1 font-mono text-caption uppercase tracking-[var(--tracking-archive)] text-secondary ${className}`}>
  <slot />
</span>

