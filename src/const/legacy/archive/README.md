# Legacy Archive Data

The archive/manga route is deprecated and retained only so older routes can build while the portfolio redesign is prepared.

Current and future project data should live in `src/const/projects.ts` with `featured` and `secondary` metadata instead of a separate archive model.
